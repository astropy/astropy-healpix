[build-system]

requires = ["setuptools",
            "setuptools_scm",
            "extension-helpers",
            "oldest-supported-numpy",
            "wheel"]

build-backend = 'setuptools.build_meta'
